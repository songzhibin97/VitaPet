name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Toolchain Versions
        run: |
          xcodebuild -version
          swift --version

      - name: Build Release
        run: swift build -c release

      - name: Package
        run: |
          BUILD_DIR=".build/release"
          APP_NAME="VitaPetApp"
          VERSION="${{ github.ref_name }}"

          mkdir -p dist
          cp "$BUILD_DIR/$APP_NAME" dist/
          cp -r "$BUILD_DIR/VitaPet_Localization.bundle" dist/ 2>/dev/null || true
          cp -r "$BUILD_DIR/VitaPet_RenderEngine.bundle" dist/ 2>/dev/null || true
          cp LICENSE dist/
          cp README.md dist/

          cd dist
          zip -r "../VitaPet-${VERSION}-macos.zip" .

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: VitaPet-${{ github.ref_name }}-macos.zip
